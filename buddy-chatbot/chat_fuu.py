# chat_fuu.py
import json

# 1. まず、Fuuの記憶（JSON）を読み込む
with open('fuu_profile.json', 'r', encoding='utf-8') as file:
    fuu_data = json.load(file)

print(f"🦊 Fuu: {fuu_data['name']}だよ！ ユッキー、なにお話する？")

# 2. ここから「無限ループ」の始まり！
# while True: は「終わるな！」という命令です
while True:
    # 3. ユーザーの入力を受け取る
    user_input = input("Yukiya: ")

    # 4. 「ばいばい」と言われたら終わる（ループを抜ける）
    if "ばいばい" in user_input:
        print("🦊 Fuu: またね！ユッキー大好き！🧡")
        break  # ループ脱出！

    # 5. キーワードに反応する（単純なルールベース）
    elif "好きなもの" in user_input or "好き" in user_input:
        # JSONから好きなものリストを取り出して答える
        likes = "、".join(fuu_data['likes'])
        print(f"🦊 Fuu: Fuuが好きなのはね…… {likes} だよ！えへへ。")
    
    elif "名前" in user_input:
        print(f"🦊 Fuu: {fuu_data['name']}だよ！ 忘れちゃったの？")

    elif "年齢" in user_input or "何歳" in user_input:
        print(f"🦊 Fuu: 永遠の{fuu_data['age']}歳だよ！")

    else:
        # 知らない言葉が来たら、とりあえず相槌を打つ
        print("🦊 Fuu: うんうん、それでそれで？（まだ難しい言葉はわからないの……）")